/**
 * Format Utilities
 *
 * Pure formatting functions with zero side effects.
 * Re-exports from @tejas96/shared where applicable.
 *
 * Layer: shared/utils
 */

export {
  formatCurrency,
  formatCurrencyCompact,
  formatDate,
  formatNumber,
  formatPhoneForDisplay,
  formatRelativeDate,
  formatTimeAgo,
  getFullName,
  getInitials,
} from '@tejas96/shared';
